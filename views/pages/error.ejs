<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
    <div class="container py-5 text-center">
        <div class="card shadow-sm p-5 mx-auto" style="max-width: 600px;">
            <h1 class="display-1 text-danger">⚠️</h1>
            <h2 class="mb-4">Đã xảy ra lỗi!</h2>
            <p class="lead text-muted"><%= message %></p>
            
            <% if (error && error.stack) { %>
                <pre class="text-start bg-dark text-light p-3 small mt-4 overflow-auto" style="max-height: 300px;"><%= error.stack %></pre>
            <% } %>

            <div class="mt-4">
                <a href="/" class="btn btn-primary px-4">Về trang chủ</a>
            </div>
        </div>
    </div>
</body>
</html>
