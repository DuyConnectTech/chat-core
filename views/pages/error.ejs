<!DOCTYPE html>
<html lang="vi">
<head>
    <%- include('../partials/head') %>
</head>
<body class="bg-light">
    <div class="container py-5 text-center">
        <div class="card shadow-sm p-5 mx-auto max-w-600">
            <h1 class="display-1 text-danger">⚠️</h1>
            <h2 class="mb-4">Đã xảy ra lỗi!</h2>
            <p class="lead text-muted"><%= message %></p>
            
            <% if (error && error.stack) { %>
                <pre class="text-start bg-dark text-light p-3 small mt-4 scroll-list"><%= error.stack %></pre>
            <% } %>

            <div class="mt-4">
                <a href="/" class="btn btn-primary px-4">Về trang chủ</a>
            </div>
        </div>
    </div>
</body>
</html>
